repositories {
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation(name:'estimote-sdk', ext:'aar')

    implementation "androidx.work:work-runtime:2.7.0"

}
