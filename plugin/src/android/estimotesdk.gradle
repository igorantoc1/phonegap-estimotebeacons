repositories {
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation(name:'estimote-sdk', ext:'aar')

    implementation 'androidx.work:work-runtime-ktx:2.7.0'
}
